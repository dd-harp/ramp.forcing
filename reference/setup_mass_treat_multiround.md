# Set Up a Bed Net Function

This sets up a function to set the value of bed net treatage

## Usage

``` r
setup_mass_treat_multiround(xds_obj)
```

## Arguments

- xds_obj:

  a **`ramp.xds`** model object
