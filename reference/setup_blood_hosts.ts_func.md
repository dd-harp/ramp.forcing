# Set up blood hosts model (ts_func)

Setup a time series function for blood host availability.

Setup a time series function for blood host availability.

## Usage

``` r
# S3 method for class 'ts_func'
setup_blood_hosts(mod_name, xds_obj, s, options)

# S3 method for class 'ts_func'
setup_blood_hosts(mod_name, xds_obj, s, options)
```

## Arguments

- mod_name:

  the model name

- xds_obj:

  an **`xds`** model object

- s:

  the vector species index

- options:

  a list to configure `F_blood_host`

## Value

an **`xds`** object

an **`xds`** object
