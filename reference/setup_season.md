# Setup Seasonal Forcing

Setup seasonal forcing

## Usage

``` r
setup_season(X, xds_obj, ix = 1)
```

## Arguments

- X:

  a seasonality function object

- xds_obj:

  an **`xds`** model object

- ix:

  the species index

## Value

an **`xds`** object
