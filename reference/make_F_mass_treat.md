# Set up dynamic forcing

If dynamic forcing has not already been set up, then turn on dynamic
forcing and set all the

## Usage

``` r
make_F_mass_treat(treat)
```

## Arguments

- treat:

  a list with parameters for all the rounds

## Value

a **`xds`** object
