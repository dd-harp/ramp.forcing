# Activate Dynamic Importation (dynamic)

No-op: the `importation_obj` is already dynamic.

## Usage

``` r
# S3 method for class 'dynamic'
dynamic_importation(xds_obj)
```

## Arguments

- xds_obj:

  an **`xds`** model object

## Value

an **`xds`** object
