# blood_hosts Dynamics

Compute and update blood host availability on the `XY_interface`.

Compute and update blood host availability on the `XY_interface`.

## Usage

``` r
blood_hosts_dynamics(t, y, xds_obj)

blood_hosts_dynamics(t, y, xds_obj)
```

## Arguments

- t:

  current time

- y:

  the state variable vector

- xds_obj:

  an **`xds`** model object

## Value

an **`xds`** object

an **`xds`** object
