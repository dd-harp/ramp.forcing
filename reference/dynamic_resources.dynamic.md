# Set up dynamic resources (already dynamic)

No-op when resources are already dynamic.

## Usage

``` r
# S3 method for class 'dynamic'
dynamic_resources(xds_obj)
```

## Arguments

- xds_obj:

  an **`xds`** model object

## Value

an **`xds`** object
