# Set Up the Null Bed Net Contact Object

Set up the null bed net coverage object

Set up the null bed net coverage object

## Usage

``` r
# S3 method for class 'linear'
setup_bednet_contact(name, xds_obj, options = list())

# S3 method for class 'linear'
setup_bednet_contact(name, xds_obj, options = list())
```

## Arguments

- name:

  the name of a model to set up

- xds_obj:

  a **`ramp.xds`** model object

- options:

  a list of options to override defaults

## Value

a **`ramp.xds`** model object

a **`ramp.xds`** model object
