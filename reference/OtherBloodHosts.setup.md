# Blood Hosts (setup)

Sets a static value for blood host availability, resets the class to
`static`, and triggers an update to the blood feeding model.

Sets a static value for blood host availability, resets the class to
`static`, and triggers an update to the blood feeding model.

## Usage

``` r
# S3 method for class 'setup'
OtherBloodHosts(t, y, xds_obj)

# S3 method for class 'setup'
OtherBloodHosts(t, y, xds_obj)
```

## Arguments

- t:

  time

- y:

  state variables vector

- xds_obj:

  an **`xds`** object

## Value

an **`xds`** object

an **`xds`** object
