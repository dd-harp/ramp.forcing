# Setup Muti-Round Bed Net coverage

With multi-round bed net coverage, there is a different relationship
between coverage and coverage in each round

## Usage

``` r
change_bednet_coverage_multiround(xds_obj, peak_access)
```

## Arguments

- xds_obj:

  an **`xds`** model object

- peak_access:

  peak access

## Value

an **`xds`** model object
