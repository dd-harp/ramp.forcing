# Set up dynamic forcing

If dynamic forcing has not already been set up, then turn on dynamic
forcing and set all the

## Usage

``` r
# S3 method for class 'func'
setup_rainfall(Tname, xds_obj, Topts = list())
```

## Arguments

- Tname:

  the name of a model to set up

- xds_obj:

  an **`xds`** object

- Topts:

  a list of options to override defaults
