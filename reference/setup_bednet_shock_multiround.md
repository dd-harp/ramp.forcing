# Setup Bed Net Shocks

Set up a function that computes bednet multi_objage over time.

## Usage

``` r
setup_bednet_shock_multiround(xds_obj, options = list())
```

## Arguments

- xds_obj:

  an **`xds`** model object

- options:

  a list of options

## Value

an **`xds`** model object
