# Setup the Blood Hosts Object

Setup the object to handle other blood hosts – potential blood hosts
that are not hosts for the pathogen

Setup the object to handle other blood hosts – potential blood hosts
that are not hosts for the pathogen

## Usage

``` r
setup_blood_host_object(xds_obj)

setup_blood_host_object(xds_obj)
```

## Arguments

- xds_obj:

  an **`xds`** model object

## Value

an **`xds`** object

an **`xds`** object
