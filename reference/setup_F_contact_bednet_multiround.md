# Make `F_contact` for bednet

Set up the bednet rounds

## Usage

``` r
setup_F_contact_bednet_multiround(xds_obj)
```

## Arguments

- xds_obj:

  a **`ramp.xds`** model object

## Value

set up the rounds
