# Set up dynamic weather

If dynamic weather has not already been set up, then turn on dynamic
weather and set up all its null models

## Usage

``` r
dynamic_weather(xds_obj)
```

## Arguments

- xds_obj:

  an **`xds`** object

## Value

an **`xds`** object
