# Set up blood hosts model

Setup a model for blood host availability.

Setup a model for blood host availability.

## Usage

``` r
setup_blood_hosts(mod_name, xds_obj, s, options)

setup_blood_hosts(mod_name, xds_obj, s, options)
```

## Arguments

- mod_name:

  the model name

- xds_obj:

  an **`xds`** model object

- s:

  the vector species index

- options:

  a list to configure `F_blood_host`

## Value

an **`xds`** object

an **`xds`** object
