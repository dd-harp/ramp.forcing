# Set up dynamic forcing

A simple model for IRS effect sizes

## Usage

``` r
make_irs_effect_sizes_simple(options = list(), contact = 1)
```

## Arguments

- options:

  a list of options to override defaults

- contact:

  the probability of contact given coverage

## Value

a **`ramp.xds`** model object
